FROM python:3.14-slim

WORKDIR /app

RUN pip install --no-cache-dir PyJWT==2.10.1 cryptography==44.0.3

# Create non-root user
RUN groupadd --system appgroup && useradd --system --gid appgroup appuser

COPY mock_oidc.py .

USER appuser

EXPOSE 9000

CMD ["python", "mock_oidc.py"]
